{
	"name": "js Node.js project with Arch and Neovim",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"remoteUser": "node",
	"workspaceFolder": "/workspace", // Ensures the workspace is properly mapped in the container
	"forwardPorts": [
		3000
	] // Forward port if your Node.js app runs on this port
}